# LyraMD / ColaMD

## 当前工作基线

**当前产品线是 LyraMD 1.x：继续基于现有 Electron + Milkdown 实现迭代，不重启 LyraMD 2.0 / VMark-based 改造。**

编码前先确认正在修改的是当前 `src/` 实现面。除非 Cherry 明确要求重启 2.0 或迁移技术栈，否则不要把新功能写成 VMark-derived 重构计划，也不要因为旧文档里出现 2.0 / VMark 方向就切走当前代码线。

## 技术栈边界

- 不要更换技术栈：继续使用 Electron、electron-vite、TypeScript、Milkdown / ProseMirror、Vitest。
- 不要把当前任务升级成 Tauri、React、VMark 或完整 IDE/知识库重写。
- 可以做局部依赖对齐、测试补强和 Electron/Milkdown 内部重构，但前提是保持现有产品可运行、可构建、可回滚。

## VMark 借鉴边界

VMark 只作为交互参考，可以借鉴：

- 更顺手的侧边栏收纳和工作区切换方式。
- 简洁、低打扰的 Agent / AI 入口。
- 宽屏右侧面板、窄屏底部抽屉或居中命令面板这类布局思路。
- 文稿切换和文件树的轻量交互。

不要借鉴成：

- 全量 VMark 功能复刻。
- 新技术栈迁移。
- 复杂知识库、完整 IDE 或重型 MCP 面板。
- 用 2.0 名义绕开当前 1.x 的可用性修复。

## 当前产品方向

LyraMD 1.x 是 Agent Native 的 Markdown 编辑器。当前重点是在已有能力上继续打磨：

- 外部 Agent 修改 `.md` 文件后的热更新。
- 基础 Markdown / WYSIWYG 编辑体验。
- 工作目录、工作区、置顶、最近文件和草稿信息架构。
- 设置、主题、背景、自定义快捷键、侧边栏和大纲。
- AI 精灵作为可预览、可撤回的选中文本辅助入口。

## 快捷键语义

- `Cmd/Ctrl+J`：打开 AI 精灵。AI 精灵先显示选区、指令和结果预览，再由用户选择替换、插入或复制。
- 不使用 `Cmd/Ctrl+Y` 做直接改写。直接替换选区没有预览，容易误触，不符合当前交互方向。
- `清理中英排版` 是确定性编辑器动作，保留在格式菜单和快捷键设置里，不放进 AI 精灵默认动作。

## 开发规范

- 编码前先找相似实现，优先复用现有模块、测试和脚本。
- 对非平凡改动，先补最小回归测试或 before/after 对照。
- 关键修改前后看 `git diff`，不要在脏上下文里盲目前推。
- UI 改动保持 1.x 当前信息架构，不做整套视觉重写。
- 完成后至少运行相关测试；涉及构建或依赖时运行 `npm run build`。
